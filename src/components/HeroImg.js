import React from "react";
import koller from "./../img/koller.png";

const HeroImg = () => {
    return (
        <div className="right-hero">
            <img src={koller} alt="" />
        </div>
    );
};

export default HeroImg;
